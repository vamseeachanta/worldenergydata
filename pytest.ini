[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py *_test.py
norecursedirs = _archived_tests .git .venv venv 2025-* legacy_* custom_scripts output legacy comprehensive-report-system financial-analysis-sme-code well-data-verification
python_classes = Test*
python_functions = test_*

markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (multiple components)
    slow: Slow tests (>1 second execution)
    llm: LLM/AI tests (external API calls)
    scrapy: Web scraping tests
    selenium: Browser automation tests
    database: Database integration tests
    api: API endpoint tests
    etl: ETL pipeline tests
    data_validation: Data validation and verification tests
    performance: Performance and benchmark tests
    benchmark: Benchmark performance tests
    smoke: Smoke tests for quick validation
    regression: Regression tests for existing functionality
    network: Tests requiring network access
    skip_ci: Tests to skip in CI/CD pipeline
    wip: Work-in-progress tests

addopts =
    -v
    --tb=short
    --strict-markers
    -x
    --cov=src
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
    --cov-fail-under=80
    --maxfail=5
    --timeout=300

asyncio_mode = auto
console_output_style = progress

filterwarnings =
    error
    ignore::UserWarning
    ignore::DeprecationWarning
