# Adversarial plan review R2 — issues #1038–#1044 — Codex

**Reviewed commit:** `2d71bcd`
**Verdict:** MAJOR
**Mode:** external Codex CLI, read-only, adversarial

The review rejected the plan set on eight remaining classes:

1. Residual-percentage interval division was invalid for negative residuals.
2. Portfolio requirement/event registries lacked downstream ownership.
3. Effective event dates were conflated with source-availability dates.
4. Outer estimator scores were reused for model-family selection.
5. Child manifests did not consistently enforce the common envelope.
6. FDAS variance could subtract incompatible bases and used an ungated threshold.
7. Native-currency quantization boundaries and rounding modes were underspecified.
8. Per-slice TDD commands still aggregated future files rather than exact node IDs.

The main session patched all eight classes and performed the required R3 closure inline rather than dispatching another Codex cycle.
