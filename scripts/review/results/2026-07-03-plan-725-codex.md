# Codex adversarial plan review - Issue #725

Plan reviewed: `docs/plans/2026-07-03-issue-725-kansas-kgs-pressure-observations.md`
Review time: 2026-07-03T01:55Z

## Verdict

APPROVE with minor implementation cautions.

The revised plan is approval-ready from a Codex review standpoint. The initial
Claude MAJOR classes were checked against the patched plan:

- The #725 virgin-pressure proxy is now explicit via
  `is_earliest_observation_for_well` and `virgin_pressure_proxy_method`, with a
  dedicated TDD test.
- The pressure unit contract now retains raw gauge-pressure provenance and
  converts to `pressure_psia` using a documented atmospheric-pressure
  assumption.
- Ambiguous API10 joins now suppress `api14`, reference depth, and gradient.
- County coverage now requires a packaged mapping plus verification fixture,
  with unknown codes retained and counted.
- Package-data shipping, annual `test_date` policy, `test_type=KS_PRORATION`,
  parser row-count repair, header trimming, all CLI registry/display sites, and
  dependency pin style are now explicitly planned.

## Findings

No blocking findings remain.

Minor cautions for the implementer:

1. The plan correctly treats `SHUT_IN_PRESS` as gauge unless contrary official
   metadata is found. During implementation, search KGS page/docs for unit
   metadata before hardcoding the assumption, and preserve the evidence in the
   manifest or docs.
2. The county-code mapping must be backed by real verification, not only a YAML
   table. The plan now requires this; implementation should make the verification
   fixture explicit and small enough for unit tests.
3. The `/mnt/ace` smoke should report whether the existing raw files already
   match the live KGS `Content-Length` and hash. If they differ, refresh or
   record the mismatch before building curated outputs.

## Checks performed

- Read the revised plan after the Claude MAJOR remediation patch.
- Checked the issue dependency logic against #725/#708/#709/#710 descriptions
  already fetched in-session.
- Verified local repo shape used by the plan:
  `packages/*` uv workspace members, explicit root dependency/source entries,
  explicit CLI registration in `src/worldenergydata/cli/main.py`, and lazy
  command module allowlist in `src/worldenergydata/cli/commands/__init__.py`.
- Verified live KGS source evidence collected in-session:
  pressure file HTTP 200, 14,017,158 bytes, malformed second line present;
  wells zip HTTP 200, 43,773,721 bytes; optional tops zip HTTP 200.
- Verified `/mnt/ace/worldenergydata/data/modules/kansas_kgs` already contains
  the required raw pressure and wells files.

## Required changes

None before moving to `status:plan-review`.
