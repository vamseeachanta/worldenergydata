# Codex-style Adversarial Plan Review — Issue #403

Verdict: **MAJOR**

The draft plan is directionally aligned with the issue, but not approval-ready because it has a likely stats-definition bug, under-specifies provenance/timestamp compliance, and risks repo-policy drift on interactivity and binary artifact bloat.

## Major findings

| Severity | Cited evidence | Risk | Required change |
|---|---|---|---|
| MAJOR | Plan pseudocode proposed broad keyword matching for `foundered`, `hatch`, and `watertight`; CSV rows include non-incident controls such as `NI002` (hatches verified secure) and `NI010` (weather routing avoided severe storm). | Weather/water statistics can be inflated or semantically misleading. | Define separate metric buckets and inclusion/exclusion rules; exclude explicit non-incident/`severity=None` controls from risk-event counts unless reported as controls. |
| MAJOR | `worldenergydata/CLAUDE.md` requires data attribution + timestamp; plan only mentioned generated timestamp in pseudocode. | Artifact may fail repo visualization/reporting standards. | Add tested `generated_utc` JSON field and visible timestamp/source attribution in HTML. |
| MAJOR | Repo rule says all visualizations are interactive HTML; draft plan read like static infographic. | Implementation may ship static poster-style HTML that violates repo expectations. | Require self-contained interactive HTML with lightweight details/tooltips or explicitly document exemption. |
| MAJOR | Existing prior PNG is ~1.0 MB and PDF is ~0.5 MB; plan preserved old binaries and optionally generated new binaries. | History bloat and duplicate binary artifacts. | Default deliverables to HTML+JSON; only preserve/generate PNG/PDF if policy-safe and explicitly approved or kept out of git. |

## Missing tests

- False-positive classifier tests for `NI002` and `NI010`.
- Determinism test for stats JSON excluding timestamp.
- Timestamp/provenance tests for JSON and visible HTML footer.
- Relative-path durability test to avoid dependence on `/home/vamsee/Downloads/...` as the only provenance reference.
- Interactive HTML contract test.
- Binary-output gating test.

## Scope creep risks

- Preserving old PNG/PDF plus creating new PNG/PDF can multiply binary footprint.
- Review artifacts and plan-index updates are workflow bookkeeping; keep them separate from the user-facing artifact definition.
- Pulling too much from the external DOCX can turn this into document transcription instead of data-backed positioning.

## Weakest assumption

That a simple keyword classifier can defensibly produce a headline `weather_water_related` statistic. The source data mixes incident rows and non-incident/control rows.

## Most likely implementation failure mode

A polished infographic with numerically inconsistent or semantically inflated weather/water stats because tests recompute the same flawed classifier instead of validating metric definitions against adversarial examples.
