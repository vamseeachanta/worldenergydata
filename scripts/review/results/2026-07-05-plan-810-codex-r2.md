# Plan Review r2: #810 Spain CORES field-development report - Codex
Verdict: MAJOR

## Findings
- [MAJOR] Plan Deliverable items 4/6 and pseudocode `adapter = SpainCoresAdapter(...)`: the Plotly dependency boundary is still broken. Importing `worldenergydata.production.unified.adapters.spain_cores_adapter` executes `packages/worldenergydata-production/src/worldenergydata/production/__init__.py:8`, which imports `worldenergydata.production.forecast`; `forecast/decline.py:16` imports `plotly.graph_objects`. I reproduced `ModuleNotFoundError: No module named 'plotly'` with the planned import path. The claim “The Python package will not import `plotly`, so `worldenergydata-spain` will not need a new Plotly runtime dependency” is not valid unless the plan also changes the production package import surface, relocates the adapter/query imports, or explicitly scopes the report to the root managed environment.
- [MINOR] Plan Boundary decisions / TDD HTML rendering: the plan says the public report is “self-contained” but later allows it to be self-contained “apart from the allowed Plotly JavaScript runtime.” `scripts/build_pages.py:491-496` describes field-development pages as self-contained HTML with inline CSS/JS and no external assets. If Plotly is CDN-loaded, the prior public-runtime blocker is only partially resolved.
- [MINOR] Plan Boundary decisions “cite ... workbook/source metadata” and Operational input root: the plan omits `metadata/cores_refresh_metadata.json` from the input contract. Current `/mnt/ace/.../_metadata.json` has row counts and file names only; workbook hashes, workbook source URLs, units, last-modified dates, and status codes are in `/mnt/ace/worldenergydata/data/spain/cores/metadata/cores_refresh_metadata.json`. The report cannot satisfy the workbook provenance claim if it only validates `_metadata.json` and `manifest.json`.
- [MINOR] `scripts/build_pages.py` change plan: adding a Spain card inside `_field_development_section()` without updating surrounding copy will misclassify the report. Current landing text says “Offshore Field-Development Playbook” and “BSEE/SubseaIQ” provenance at `scripts/build_pages.py:790-795` and `scripts/build_pages.py:974-979`; Spain CORES is not BSEE/SubseaIQ and includes onshore Ayoluengo.

## R1 Resolution Check
- Normalized CSV read path vs raw/download side effects: resolved in plan. Verified `CoresLiveProductionLoader` reads/downloads raw workbooks and writes normalized CSVs (`cores_live.py:67-83`, `93-109`), while the revised plan adds a normalized-CSV loader and sentinel tests.
- Environment/onshore metadata for `onshore_model_mismatch`: resolved for Ayoluengo. Verified `reference_chain.py:82-86` treats `environment="onshore"` as onshore and tests already assert the mismatch flag.
- JSON sidecar vs self-contained public HTML runtime: partially resolved. Runtime fetch of `spain_cores.json` is explicitly prohibited, but external Plotly runtime remains ambiguous.
- Spain package Plotly dependency boundary: not resolved. Planned adapter import still triggers Plotly through production package initialization.

## Verified Checks
- Live cache proof matched the plan exactly: 4,375 all rows, 2,865 oil rows, 1,520 gas rows, 20 fields, Ayoluengo 688 rows, manifest success.
- GitHub check: #810 is OPEN with `status:needs-plan`; #809 is CLOSED with `status:done`; open PR list is empty.
- Checked `cores_live.py`, `spain_cores_adapter.py`, `reference_chain.py`, `test_reference_chain.py`, `build_pages.py`, `tests/test_build_pages.py`, Spain/production pyprojects, and live cache metadata files.
- Verified `docs/plans/_template-issue-plan.md` and `scripts/data/drive-index-search/search.py` are absent in this checkout.

## Residual Risks
- Add a fail-closed consistency check that loaded CSV row count matches `_metadata.record_count` and `manifest.records_updated`; the current plan validates presence/status but not cross-file consistency.
- Decide whether public Plotly is inline/vendored or externally loaded; tests should enforce the chosen runtime contract.
- Cleanup audit: expected draft plan/index changes are present; pre-existing stash and sibling worktrees were observed. No files were edited.