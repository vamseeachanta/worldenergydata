# Main-Session Adversarial Resolution: worldenergydata #924, r3

**Resolution verdict: NO UNRESOLVED MAJOR - READY FOR `status:plan-review`**

This is the required r3 loop-break pass after the usable r1/r2 reviews surfaced different defects. The main session patched and challenged the revised plan inline and did not dispatch another provider round. This verdict does not approve implementation. Explicit user approval remains mandatory.

## Finding Resolution

| Codex r2 finding | Resolution in revised plan |
|---|---|
| Search filtering undefined | Exact normalized state/county matching, owner/legal substring matching, AND composition, record-ID order, and empty success are fixed and tested |
| Singular atomic error envelope | `requested_provider`, nullable `resolved_provider`, ordered `failures[]`, and `provider_by_operation` cover single/multi-operation states without inventing a provider |
| Custom-path TOCTOU | Custom input is a direct-child basename only; CWD and child are descriptor-opened, `O_NOFOLLOW` is mandatory, and fstat/read use the same descriptor |
| NaN/bool numeric admission | `parse_constant` rejects JSON constants; booleans and non-finite values are rejected before numeric conversion |
| In-process network proof only | An active temporary `sitecustomize` socket guard marks loading, records attempts, and gates the external module subprocess |
| CLI split compatibility absent | Tests inventory search, lookup, county-info, providers, status, current options/help, and table/JSON/CSV/output-file modes |
| Runtime readiness schema absent | Exact row keys, `--operation`, explicit source context, requirement semantics, route-mode separation, and non-selecting sample availability are fixed |
| Installed package not proven | Root, core, and landman wheels are installed into an external venv and invoked from an empty CWD outside the checkout |
| Environment count mislabeled | Evidence now preserves uv's `Installed 233 packages` wording and separately reports a verified 234 distributions / 39 `worldenergydata*` distributions |
| Review paths stale | Header/artifact map cite revision-stamped fallback and r3 artifacts |

## Additional Generalizable Finding

The repository lacks the scorer, renderer, close workflow, and completeness labels required by the workspace lifecycle. That defect was promoted to [workspace-hub #3426](https://github.com/vamseeachanta/workspace-hub/issues/3426). It blocks #924 closeout, not user-approved TDD implementation.

## Verification Evidence

- Plan length: 324 lines at the first r3 check, below the 400-line ceiling; later edits remained bounded and were rechecked before commit.
- Required headers: `Client: N/A`, `Lane: lane:codex`; live issue has exactly one matching lane.
- Live issue state: OPEN, `status:needs-plan`, no approval label.
- Local metadata probe: 234 installed distributions; 39 `worldenergydata*` distribution names.
- Existing CLI inventory verified five commands and current state/county/section/township/range/owner/type/provider/format/output options before defining split-compatibility tests.
- `git diff --check` passed for the plan and r2 fallback artifact.
- `legal-sanity-scan --diff-only` reported no scannable files because this repo's policy excludes the changed Markdown/review paths; implementation code will still require the legal gate.
- Stale-contract search found no old root-CLI-test, 233-workspace-package, resolved-path, or singular-provider contract language.

Implementation remains blocked until the pushed plan is placed in `status:plan-review` and the user explicitly approves it.
