Ignoring 19 permissions.allow entries from .claude/settings.json: this workspace has not been trusted. Run Claude Code interactively here once and accept the trust dialog, or set projects["/mnt/local-analysis/worldenergydata"].hasTrustDialogAccepted: true in /home/vamsee/.claude.json.
Review complete. I verified the plan's claims against the #702 sibling plan, the implemented `dossiers` package, the live `/mnt/ace` dossier packet, the GitHub issue, and repo CI config.

Verdict: MAJOR

Findings:

- [MAJOR] Pseudocode / TDD list / Acceptance Criteria — fail-closed publication contract regresses from the #702 precedent. The pseudocode (plan lines 326–343) early-returns on `dry_run` and then raises only `if blocking_gaps and require_sources`, so a plain non-dry-run publication with a missing dossier index/manifest/quality file proceeds to build and write. The implemented #702 contract is `if blocking_source_gaps and (require_sources or not dry_run): raise` (`packages/worldenergydata-texas_rrc/src/worldenergydata/texas_rrc/dossiers/cli_support.py:55`) — every non-dry-run publication fails on blocking gaps, and `--require-sources` additionally fails dry-runs. Test 18 pins only the `--require-sources` case, and no test covers plain publication with blocking gaps. There is also no empty-publication guard: #702 had `test_run_build_field_architecture_dossiers_rejects_empty_publication`; this plan would let a 0-row dossier index publish an empty portfolio packet. Required fix: adopt the #702 gap semantics verbatim, add tests for non-dry-run failure without `--require-sources`, dry-run failure with `--require-sources`, and rejection of empty action-queue publication.

- [MAJOR] Action queue columns / Summary tables — output-contract fields with no defined derivation, vocabulary, or ordering. `followup_priority` and `review_sequence` appear in the action-queue column list (plan lines 203, 205) but are defined nowhere in the plan; they are not in the live #702 index (verified against the 40 actual index columns) and no mapping, value set, or computation is given. Test 7 requires deterministic ranking "by action priority" but the priority order across the six portfolio actions is never enumerated, so the ranking is unimplementable-as-specified and unreviewable. In the class summary, `direct_or_near_access_count` (line 263) never states which classes count — the actual vocabulary is `direct_access`, `near_access`, `regional_access`, `remote_access`, `isolated_or_unknown`, `not_available` (`infrastructure/access_metrics.py:437-448`) — and `top_caveats`/`top_quality_flags` never define "top" (by what count, how many). Required fix: enumerate the action-priority order, define `followup_priority` and `review_sequence` derivations and vocabularies (or drop the columns), pin the access classes counted, and define the top-caveat/flag rule.

- [MINOR] Pseudocode / TDD test 19 — the non-ACE output guard is not pinned. The #702 plan specified the exact parameter chain (`allow_non_ace_root` at I/O, `allow_non_ace_output` at CLI support, `--allow-non-ace-output` flag); this plan's pseudocode signature omits the parameter entirely, the CLI flag set is never listed, and test 19 says only "when the test helper enables that path". Required fix: state the flag/parameter names and rejection behavior, matching the existing dossiers/opportunities convention.

- [MINOR] Validation Gates — lint commands diverge from PR CI. The gate runs black + ruff, but CI runs black + isort + flake8 on `src/` and `tests/` (`.github/workflows/ci.yml:160-167`), which covers the six new test files and the modified `src/worldenergydata/cli/commands/texas_rrc.py`. Ruff is present in `uv.lock` so the command will execute, but the gate can pass while PR CI fails on isort/flake8. Required fix: add the CI's isort and flake8 invocations (or replace the lint block with the exact CI commands) so gate-green implies CI-lint-green.

- [MINOR] HTML report §, relative-link algorithm — the resolution anchor for `dossier_path` is unspecified. The live index stores dossier-dir-relative values (`fields/05-00870500-...-dossier.html`); step 4 says "the row `dossier_path` resolves under the allowed dossier pages directory" without stating what the relative path is resolved against. Anchoring at `portfolio_dir` (the natural reading of steps 1–2) would fail containment for every row and silently degrade all 37 links to escaped text while still "passing" the fail-closed tests. Required fix: state that `dossier_path` is resolved against `analysis_root / "field_architecture_dossiers"` before the containment check, and pin one live-shaped fixture value in test 13.

- [MINOR] Plan structure — no "Out Of Scope" section and no per-task breakdown, both present in the #702 sibling plan. The interpretation policy covers the reserves/economics boundary, but there is no explicit exclusion list (raw refresh, PatchOps/third-party ingestion, per-well allocation, statewide rollups beyond the note in the selection paragraph, public publishing outside `/mnt/ace`), and the single flat TDD list gives no task-ordered failing-test/verification sequence. Required fix: add an Out Of Scope section; a task breakdown is recommended but optional if the TDD list is kept authoritative.

Checked:
- `docs/plans/2026-07-02-issue-707-texas-rrc-field-architecture-portfolio.md` (full read) and `docs/plans/README.md` index row (#707 draft, consistent).
- `docs/plans/2026-07-02-issue-702-texas-rrc-field-architecture-dossiers.md` (full read) for contract precedent.
- GitHub issue #707 via `gh issue view` — plan scope, output path, boundaries, and acceptance shape match the issue body; screening-only boundary language matches (no reserves/economics/tariffs/capacity/ROW/route/facility-design claims — plan is clean on that axis).
- `dossiers/io.py` — all six constants claimed by the plan exist (lines 26–34); `dossiers/__init__.py` exports only the input loader, confirming the plan's "no output loader exposed" claim.
- `dossiers/cli_support.py:55` — actual #702 fail-closed condition, basis of the MAJOR contract finding.
- Live `/mnt/ace/.../field_architecture_dossiers/` packet: directory listing, `manifest.json` (nested `quality.blocking_source_gaps`/`informational_source_gaps` present, `input_paths` present — plan's normalization order works against real data; row_count 37 and class distribution match the plan's tables exactly), `quality.json`, and the index CSV header (40 columns — all plan-referenced source columns present except the two undefined derived ones).
- `src/worldenergydata/cli/commands/texas_rrc.py:1255` — `build-field-architecture-dossiers` command exists as claimed.
- `.github/workflows/ci.yml:160-167` (black/isort/flake8, no ruff), `pyproject.toml` optional dev deps, `uv.lock` (ruff present as a transitive dependency).
- `scripts/legal/legal-sanity-scan.sh` exists.
- `infrastructure/access_metrics.py:437-448` — access-class vocabulary for the `direct_or_near_access_count` finding.

`★ Insight ─────────────────────────────────────`
- The highest-value defect here came from diffing the plan against the sibling module's *implemented* behavior rather than its plan text — the #702 plan and code agree on fail-closed semantics, and #707's pseudocode quietly weakens them, which a plan-only read would miss.
- Live-artifact probes matter for link contracts: the `dossier_path` values on disk are dossier-dir-relative, which turns an "obviously fine" containment check into an all-links-degrade bug depending on the unstated anchor.
`─────────────────────────────────────────────────`
