**Findings**

- **MAJOR**: The staged self-contained report is not actually runnable. [spain_cores.html](/mnt/local-analysis/wt-wed-810-spain-report-plan/reports/field_development/spain_cores.html:87) embeds literal `NaN` values inside the `<script type="application/json">` payload, then [cores_html.py](/mnt/local-analysis/wt-wed-810-spain-report-plan/packages/worldenergydata-spain/src/worldenergydata/spain/reports/cores_html.py:145) calls `JSON.parse(...)`. I reproduced this with Node against the staged HTML; `JSON.parse` exits nonzero. The sidecar has the same issue, starting at [spain_cores.json](/mnt/local-analysis/wt-wed-810-spain-report-plan/reports/field_development/spain_cores.json:63), so the “durable JSON” is not standards-compliant JSON for strict consumers. Root cause is [cores_field_development.py](/mnt/local-analysis/wt-wed-810-spain-report-plan/packages/worldenergydata-spain/src/worldenergydata/spain/reports/cores_field_development.py:209), where `row.get(...) or 0.0` does not coerce pandas `NaN`, and [cores_html.py](/mnt/local-analysis/wt-wed-810-spain-report-plan/packages/worldenergydata-spain/src/worldenergydata/spain/reports/cores_html.py:81) / [cores_field_development.py](/mnt/local-analysis/wt-wed-810-spain-report-plan/packages/worldenergydata-spain/src/worldenergydata/spain/reports/cores_field_development.py:348) serialize with default `json.dumps` behavior. This blocks the acceptance requirement for self-contained HTML with all-field monthly visibility.

**Residual Risks/Test Gaps**

- The focused tests use a synthetic cache with complete oil/gas columns, so they miss the real merged CORES shape where oil-only rows have missing gas and gas-only rows have missing oil. Add a fixture with `NaN`/missing counterpart values and assert the HTML payload can be parsed by `JSON.parse` and the JSON sidecar contains no `NaN`.
- Metadata validation is materially improved versus prior r1: manifest status/job/records and workbook fields/status are checked. Remaining gaps: no negative tests for workbook `status_code >= 400`, malformed `sha256`, nonpositive `byte_count`, missing `_metadata.source_url`, or missing `_metadata.last_refresh`.
- Pages filtering looks correct: [scripts/build_pages.py](/mnt/local-analysis/wt-wed-810-spain-report-plan/scripts/build_pages.py:505) publishes only `spain_cores.html` as `spain-cores.html`; JSON sidecar is not copied. `legal-sanity-scan: PASS`; `git diff --cached --check` clean.

Final Verdict: MAJOR