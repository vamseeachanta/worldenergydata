# Plan Review r3: #810 Spain CORES field-development report - Codex
Verdict: MINOR

## Findings
- [MINOR] `docs/plans/2026-07-05-issue-810-spain-cores-field-development-report.md` Deliverable item 6: the claim “The report will not import Python Plotly” is still too broad. The planned `SpainCoresAdapter` import still enters `packages/worldenergydata-production/src/worldenergydata/production/__init__.py:8`, which imports forecast code, and `packages/worldenergydata-production/src/worldenergydata/production/forecast/decline.py:16` imports `plotly.graph_objects`. Declaring `worldenergydata-production` fixes the missing dependency in the managed environment, but it does not make the Python import path Plotly-free.
- [MINOR] `docs/plans/...` Source validation: the plan says `metadata/cores_refresh_metadata.json` records workbook “status” and tests “non-success workbook status”; the actual schema emits `status_code` at `packages/worldenergydata-spain/src/worldenergydata/spain/production/cores_source.py:270`. Tests should validate `status_code == 200` or explicitly normalize the schema.
- [MINOR] `docs/plans/...` Public-pages tests: the plan requires not labeling Spain as BSEE/SubseaIQ/offshore-only, but the listed test only requires a Spain card when copied. Current `scripts/build_pages.py` still has global BSEE/offshore/SubseaIQ landing copy, so add a negative assertion against those labels around the Spain card/section.

## R2 Resolution Check
- Production adapter/reference-chain import path and Plotly/Python boundary: partially resolved. Runtime dependency is acknowledged, but “will not import Python Plotly” remains false for the planned adapter import path.
- Self-contained public HTML runtime vs external Plotly/JSON fetch: resolved. The revised plan requires inline SVG/JS, no CDN Plotly, and no `spain_cores.json` fetch.
- Workbook metadata input contract: mostly resolved. `metadata/cores_refresh_metadata.json` is now required, but the status key needs correction to `status_code`.
- `build_pages` landing copy mislabeling: mostly resolved in acceptance text, under-tested.
- Row count consistency and template brace-safety tests: resolved. The plan now includes `_metadata.record_count`/`manifest.records_updated` matching and sentinel/Template brace-safety tests.

## Verified Checks
- Read the local plan file and Codex r2 artifact.
- Reproduced the adapter import failure in unmanaged Python: missing `plotly`.
- Verified `SpainCoresAdapter` supports a duck-typed normalized loader.
- Verified `CoresLiveProductionLoader` reads/downloads raw workbooks and writes normalized CSVs, so avoiding it remains necessary.
- Verified workbook metadata schema fields include `status_code`, `sha256`, `byte_count`, `last_modified`, and `source_url`.
- Verified current `scripts/build_pages.py` landing copy still contains BSEE/offshore/SubseaIQ framing.

## Residual Risks
- Live `/mnt/ace` row counts were not re-run in this sandbox; implementation should rerun the fixture/live proof before generating tracked artifacts.
- The plan should distinguish “renderer does not use Plotly” from “Python process never imports Plotly.”