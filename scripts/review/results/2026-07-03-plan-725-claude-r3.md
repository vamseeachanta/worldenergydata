# Claude focused re-review r3 - Issue #725

Plan reviewed: `docs/plans/2026-07-03-issue-725-kansas-kgs-pressure-observations.md`
Review time: 2026-07-03

## Verdict

MINOR

No remaining MAJOR blocks moving the issue from `status:needs-plan` to
`status:plan-review`; the three prior MAJOR findings are affirmatively
remediated. The two MODERATE items are documentation/limitation corrections
that should land before or during implementation, not plan-gating blockers.

## Findings

- MODERATE - Direct-source inventory records `Last-Modified: Thu, 27 Mar 2025
  17:32:01 GMT` for `kansas_proration_pressures.txt`, yet the Risks section
  still asserted "KGS pressure data is frozen at 2013" and the plan would
  "manifest this cadence." The implementation should derive the actual latest
  observation year from parsed data and drive the cadence label from that.
- MODERATE - Virgin-pressure proxy temporal validity needs an explicit caveat.
  The live sample rows start at `YEAR=1996`, and Hugoton first production
  predates the proration program by decades, so the flag represents the
  earliest available proration-year pressure, not measured initial reservoir
  pressure.
- MINOR - The fixed `atmospheric_pressure_psi = 14.7` assumption is a sea-level
  screening constant. The plan should record that it does not adjust for
  Hugoton-area elevation.
- MINOR - The Hugoton/Panoma coverage-dominance sanity check is present in Task
  4 prose but should have matching TDD coverage.
- MINOR - When API10-to-API14 join ambiguity leaves `api14` null, the earliest
  proxy identity can collapse to API10. The plan should clarify whether the
  proxy flag is suppressed or qualified for ambiguous-identity rows.

## Checks performed

- Verified MAJOR #1: schema adds `is_earliest_observation_for_well` and
  `virgin_pressure_proxy_method`; policy and TDD coverage are present.
- Verified MAJOR #2: policy retains `pressure_psig_raw`, emits
  `pressure_psia = pressure_psig_raw + 14.7`, records an assumption limitation,
  and tests the 47.3 psig to 62.0 psia conversion.
- Verified MAJOR #3: ambiguous API10 joins suppress `api14`,
  `reference_depth_ft`, and `gradient_psi_ft`; TDD coverage exists for
  ambiguous joins and positive-depth gradient gating.
- Cross-checked issue acceptance criteria: raw manifest, normalized parquet row
  counts, #709 schema, county/year coverage, parser-quirk tests, and no large
  git-tracked data all map to plan tasks and acceptance criteria.
- Checked parser-quirk coverage against issue gotchas: malformed header line,
  Oracle dates, API normalization, and KGS KID fallback.
- Reconciled source `Last-Modified` metadata against frozen-cadence language
  and flagged the contradiction.
- Confirmed WHP-vs-BHP handling: `pressure_kind=WHP_shut_in`,
  `gradient_method=whp_over_total_depth_screening_only`, and gas-column
  correction deferred to #710.
