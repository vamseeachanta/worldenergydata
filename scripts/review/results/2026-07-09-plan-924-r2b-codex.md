# Adversarial Plan Review: worldenergydata #924, r2 fallback Codex

**Verdict: MAJOR**

This revision-stamped artifact preserves the usable Codex subagent review after the canonical CLI review path failed. The reviewer was instructed to assume defects, avoid praise/restatement, verify cited claims, and prefer a non-approval verdict when uncertain.

## Findings

1. **Fixture search:** filtering behavior is undefined and untested. State/county normalization, owner/legal matching, predicate composition, ordering, and no-match behavior need an exact contract.
2. **Atomic errors:** the singular example invents one provider and cannot represent unresolved or multi-operation failure. The envelope needs `requested_provider`, nullable `resolved_provider`, and ordered per-operation `failures[]`.
3. **Custom-file security:** resolving a root plus `O_NOFOLLOW` on the final path leaves attacker-controlled intermediate components and a TOCTOU gap. Use a descriptor-relative, race-safe policy or forbid subdirectories.
4. **Numeric validation:** normal float coercion can admit booleans and non-finite JSON constants. Reject JSON constants, booleans, and non-finite values before validators.
5. **No-network proof:** an in-process mock does not prove the end-to-end module subprocess is offline. The subprocess must load an independently verifiable socket guard.
6. **CLI split compatibility:** tests do not preserve all current commands, option names, help, table/JSON/CSV modes, and output-file behavior.
7. **Runtime readiness:** the exact status row schema and requirement semantics are missing. Packaged sample availability must not imply that the sample was selected.
8. **Packaging:** source-tree/importlib tests do not prove that the fixture exists in an installed wheel outside the checkout.
9. **Reproduction evidence:** `Installed 233 packages` was mislabeled as 233 workspace packages. The plan must preserve the command's wording and separate installed-distribution/workspace-package counts.
10. **Review evidence:** the header cites stale mutable paths and lacks revision-stamped r2/final resolution artifacts.

No implementation is authorized by this review.
