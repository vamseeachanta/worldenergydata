# Plan Review: #810 Spain CORES field-development report - Codex
Verdict: MAJOR

## Findings
- [MAJOR] Plan `Boundary decisions` / `Acceptance Criteria`: the claim that `onshore_model_mismatch` will be prominent “for onshore fields” is not implementable from the planned live input. `/mnt/ace/worldenergydata/data/spain/cores/normalized/cores_all_production.csv` has only `field_name`, `year`, `month`, `oil_bbl`, `gas_mcf`; `packages/worldenergydata-spain/src/worldenergydata/spain/reference_chain.py` sets the flag only from `field_meta["environment"]` / `environment_type`. The plan’s `field.to_sparse_meta()` never defines where live field environment metadata comes from, so the report can silently miss the required caveat.
- [MAJOR] Plan `Boundary decisions`: “may embed a compact inline JSON payload or reference a sibling tracked JSON summary” conflicts with `Deliverable` item 9, which publishes only `spain-cores.html`. Current `scripts/build_pages.py` has no `spain_cores.html`/`spain_cores.json` handling, and existing field-development publishing does not copy arbitrary JSON sidecars. If the implementation chooses sibling JSON, the public page can break.
- [MINOR] Plan `Reproduction proofs`: “`plotly` is declared by `packages/worldenergydata-production/pyproject.toml`” is not enough for a report package under `worldenergydata-spain`. Verified `packages/worldenergydata-spain/pyproject.toml` lacks `plotly` and lacks `worldenergydata-production`; root `pyproject.toml` masks that in repo-managed runs. The plan should either add explicit Spain package dependencies or commit to no Python Plotly import.
- [MINOR] Plan `Current code surfaces`: the citation to `docs/HTML_REPORTING_STANDARDS.md` omits its mandatory relative-CSV data-import language. The plan allows inline/sibling JSON only. Either align the artifact contract with the standard or document a deliberate self-contained-report exception.

## Verified Checks
- Verified live cache counts match the plan: 4,375 all rows, 2,865 oil rows, 1,520 gas rows, 20 fields, Ayoluengo 688 rows, scheduler manifest success.
- Verified `run_spain_reference_chain(...)`, `SpainCoresAdapter`, and existing Ayoluengo mismatch tests exist.
- Verified current `scripts/build_pages.py` does not contain `spain_cores.html` or `spain-cores.html` handling.
- Verified local worktree was not edited by this review.

## Residual Risks
- GitHub issue/PR statuses in the plan were not rechecked live.
- Existing local residue is pre-existing: modified `docs/plans/README.md`, untracked plan file, and one stash. Review made no file changes.

## Plan Text
Reviewed inline plan text from the prompt.