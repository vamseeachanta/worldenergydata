# Adversarial Re-review — Issues #354 and #355

## Issue #354

Verdict: MAJOR before final patch.

Prior MAJOR findings were largely resolved, but one blocking inconsistency remained: the plan defined `reference_data` as a valid `catalog_status` in derivation rules and risk mitigation, while the proposed manifest schema enum omitted `reference_data`. Because #354's core deliverable is the reconciled capability/data taxonomy, this mismatch had to be fixed before plan-review handoff.

Resolution applied after this re-review: add `reference_data` to the proposed `catalog_status` schema enum and tighten `MODULE_INDEX.md` generation choice.

## Issue #355

Verdict: APPROVE for plan-review readiness.

Prior MAJOR findings were resolved enough for planning handoff. Non-blocking nit: a summary row and file table still implied `docs/COMMANDS.md` rename/move remained possible, while Phase 3 correctly chose banner-only. Resolution applied after this re-review: normalize those remaining references to banner-only.
