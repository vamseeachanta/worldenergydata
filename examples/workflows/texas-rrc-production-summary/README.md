# Texas RRC Production Summary

Offline Texas RRC workflow using the module's CSV loader over bundled production
records. The route is guarded by `data.source: csv` so normal API behavior is
unchanged.
