# Pipeline Safety FFS

Offline pipeline fitness-for-service workflow using bundled synthetic defect records.

The case drives `PipelineSafetyWorkflow.generate_report` with Modified B31G and
asserts one `accept`, one `monitor`, and one `replace` verdict.
